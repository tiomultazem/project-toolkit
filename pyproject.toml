[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "project-toolkit"
version = "0.1.0"
requires-python = ">=3.10"

[project.scripts]
pt = "pt.cli:main"

[tool.setuptools]
packages = ["pt"]